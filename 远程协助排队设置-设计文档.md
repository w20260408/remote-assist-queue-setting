# 远程协助排队设置 - 设计文档

## 需求背景

当前客服端支持设置"允许多少人排队到自己名下"，但需要增加管理员统一设置功能：
- 管理员可在后台统一设置所有客服人员的排队人数上限
- 客服人员无法自行修改该设置

## 页面位置

放置在"客服端"设置页面，紧跟在现有客服端设置模块下方。

## 设计方案

### 新增模块：客服端排队人数设置

```
┌─────────────────────────────────────────────────────────────┐
│  客服端排队人数设置                                    [?]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  设置方式                                           │   │
│  │                                                     │   │
│  │  ○ 允许客服人员自行设置排队人数上限                 │   │
│  │                                                     │   │
│  │  ● 由管理员统一设置排队人数上限                     │   │
│  │                                                     │   │
│  │    统一排队人数上限： [ 5 ] 人                      │   │
│  │                      ↑ 数字输入框                   │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  说明：                                                     │
│  • 选择"允许客服人员自行设置"时，客服人员可在个人设置中     │
│    调整排队人数上限                                         │
│  • 选择"由管理员统一设置"时，所有客服人员使用统一的上限值   │
│    且无法自行修改                                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 交互说明

### 1. 设置方式单选
- **允许客服人员自行设置排队人数上限**
  - 选中后，下方数字输入框隐藏
  - 客服人员可在个人设置页面自行调整排队人数
  
- **由管理员统一设置排队人数上限**
  - 选中后，显示数字输入框
  - 输入框默认值：5（可根据业务调整）
  - 输入范围：1-50
  - 所有客服人员统一使用该数值，个人设置页面该选项置灰不可编辑

### 2. 数字输入框
- 类型：数字输入
- 默认值：5
- 最小值：1
- 最大值：50
- 单位：人
- 校验：输入超出范围时提示"请输入1-50之间的数字"

### 3. 保存逻辑
- 点击页面顶部"保存"按钮时一并保存
- 切换设置方式后立即生效
- 从"允许自行设置"切换到"统一设置"时：
  - 所有客服人员的排队人数上限更新为统一设置的值
  - 客服个人设置页面的该选项变为只读状态

## 视觉规范

### 样式参考
- 与现有"客服端机器人设置"、"客服端远程评价设置"等模块保持一致
- 标题：14px，#333，加粗
- 单选框：标准蓝色主题色
- 输入框：与"机器人选择"下拉框同宽，约 200px
- 说明文字：12px，#999

### 位置
在页面中的插入位置（参考原页面结构）：
```
...
客服端展示客服组设置
─────────────────────────
[新增] 客服端排队人数设置  ← 插入位置
─────────────────────────
[保存按钮]
```

## 数据字段

```json
{
  "remote_assist_queue_setting": {
    "mode": "admin_uniform | agent_self",  // 设置方式
    "uniform_limit": 5                      // 统一设置时的排队人数上限
  }
}
```

## 权限控制

| 角色 | 权限 |
|------|------|
| 管理员 | 可修改设置方式，可设置统一上限值 |
| 客服人员 | 仅当 mode=agent_self 时可修改个人排队上限 |

## 兼容性

- 存量数据：默认 mode=agent_self，保持现有行为不变
- 新功能上线后，管理员需手动切换到统一设置模式
